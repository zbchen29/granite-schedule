Clone and run `schedule_gen.py` to generate schedules. Example:

```
python -m pip install -r requirements.txt
python schedule_gen.py
Input the schedule year: 2025
Calendar schedule saved to: schedule_2025.pdf
```
